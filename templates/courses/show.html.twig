{% extends 'base.html.twig' %}

{% block title %}Course: {{ course.getTitle() }}{% endblock %}

{% block body %}
    <h1>{{ course.getTitle() }}</h1>
    <p>
        {{ course.getContent() | nl2br }}
    </p>
{% endblock %}
